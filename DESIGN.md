# Design

<!-- repository-creator:managed:start -->

## Role

ユーザーがどう見て、理解し、操作するかを扱う。機能は `FEATURES.md`、提供内容と文言は `CONTENT.md` の責務とし、重複して定義しない。

## Goal

プロダクト固有の目的、利用者、内容に合ったプロダクトレベルの UI / UX を作る。実在するプロダクトを具体的に観察し、一般的なAI生成UIの寄せ集めを避ける。

## Design Principles

- プロダクト、対象ユーザー、主要タスク、現在のUIを先に理解する。
- 既存UIがある場合は有効な部分を維持し、全面刷新をデフォルトにしない。
- 使いやすさ、情報階層、アクセシビリティ、既存部分との整合性、モバイル操作を装飾より優先する。
- 実装前に候補を比較し、プロダクト上の改善効果を説明できるものだけを採用する。
- 不要なカードやコンテナ、過剰な角丸・pill・badge、紫青系グラデーション、glass・glow、巨大Hero、均質なカードグリッド、UIライブラリのデフォルト感、genericなSaaSコピーを理由なく採用しない。

## Reference Research

- プロダクトレベルへの品質向上では、実装前にAI自身が実在する複数の参考元を探す。
- サイトには実際に公開・運用されているサイト、アプリには実在するアプリ、公開デモ、公式製品ページ、公式スクリーンショットを優先する。
- プラットフォーム、対象ユーザー、主要タスク、情報量が近い参考元を選ぶ。見た目だけのギャラリーや架空UIを唯一の根拠にしない。
- ブラウザを利用できる場合は、関連画面、主要フロー、各種状態、スマートフォンとPCでの変化を実際に操作・観察する。
- 参考元をそのまま模倣せず、複数の実例からこのプロダクトに必要な原則、数値感、構成上の判断だけを抽出する。

## Reference Analysis

- 各参考元について、何を参考にするかをAI自身で選び、理由とともに記録する。今回重要な観点を次から選ぶ。
  - 情報階層、ナビゲーション、主要操作の位置と流れ。
  - グリッド、コンテンツ幅、版面率、画面占有率、ファーストビュー内の情報配分。
  - 余白、間隔、揃え方、境界、セクション間のリズム。
  - テキスト密度、行長、行間、見出し比率、文字サイズ、ウェイト、書体の役割分担。
  - 色彩、背景と前景の比率、アクセント色の使用量、コントラスト。
  - コントロールやカードの密度、角丸、線、影、アイコン、画像。
  - スマートフォンとPCでの再配置、省略、固定、スクロール、タップ領域。
  - 空、読込中、成功、失敗、無効、フォーカス、ホバーなどの状態表現。
  - 動き、遷移、フィードバック、アクセシビリティ。
- 可能ならコンテンツ幅、余白、文字サイズ、行間などを測定または推定する。数値は機械的に複製せず、対象プロダクトへ合わせる。
- 採用する要素と採用しない要素を分け、目的、内容、利用場面から説明できる判断だけを残す。

## Reference Assets

- 参考調査がデザイン判断へ影響する場合だけ、対象リポジトリ直下に `design-reference/` を作る。不要なら作らない。
- `design-reference/README.md` に参考元の名称、URL、取得日、対象画面、画面幅や状態、ローカルファイル名、参考にする観点と理由、採用しない要素を記録する。
- ブラウザや画面取得を利用できる場合は、判断に使った画面のスクリーンショットを保存する。
- 公開画面のHTML、CSS、DOMは、分析上必要で保存が許される場合に限り、関係する短い抜粋だけを出典と用途付きで保存してよい。サイト全体、独自コード、素材を複製しない。
- ログイン、課金、アクセス制限を回避しない。秘密情報、個人情報、再配布できない画像やコードを保存しない。その場合はURLと分析結果だけを残す。
- 最終判断の再現に必要な参考画面と最終成果物画面だけを維持し、大量の候補や中間スクリーンショットを残さない。

## Comparative Visual QA

- UI変更後は、参考元と比較可能な主要画面・状態を、可能な限り同等の画面幅で表示してスクリーンショットを撮る。
- 見た目の類似ではなく、記録した観点に対して、情報階層、版面率、画面占有率、余白、テキスト密度、色彩、タイポグラフィ、操作の明瞭さが意図どおり反映されたかを比較する。
- 比較で見つかった問題を修正し、再表示、再撮影、再比較する。重大な差やAI生成らしい画一性が残る間は完了にしない。
- スマートフォン幅とPC幅の両方を確認し、主要フローを実際に操作する。利用可能なブラウザ、Vision、スクリーンショット手段は省略しない。
- 利用できない確認は未検証として明示する。

<!-- repository-creator:managed:end -->

## Project Direction

- Product / users / primary task: コマンドライン未経験者が、現在のミッションを読み、疑似ターミナルへ1コマンドずつ入力し、結果を理解して8ステップを完了する。
- Desired impression: 紙のフィールドノートと安全な計器盤。冷静、明快、探索的で、いわゆるハッカー風の威圧感を避ける。
- Reference products / source URLs: CMD Challenge（https://cmdchallenge.com/）、Linux Survival（https://linuxsurvival.com/linux-tutorial-introduction/）、Codecademy Learn the Command Line（https://www.codecademy.com/learn/learn-the-command-line）。取得画面と分析は`design-reference/`に記録。
- Selected reference dimensions / what to borrow: 課題と入力欄の近さ、説明とシミュレーターの並列、構造を伝える番号と罫線を採用。参考元の販促要素やブランド表現は持ち込まない。
- What not to copy / avoid: ネオンやglowを多用したサイバーパンク表現、黒一色、紫青グラデーション、glass UI、巨大Hero、過剰なカード分割、長大な常時表示サイドバー、genericなSaaS風コピー。
- Durable design decisions: Path Railを記憶点とし、現在地の変化を路線図として表示する。冷たい鉱物色、信号オレンジ、経路の青緑を使う。1画面内に進捗、レッスン、疑似ターミナル、フィードバックを置く。375px幅で横スクロールなし、モバイル初期表示は自動フォーカスしない。成功・失敗は色だけで区別せず、ラベル、フォーカス、状態通知、reduced motionへ対応する。PC初期・完了画面と375px幅をVisual QA済み。
