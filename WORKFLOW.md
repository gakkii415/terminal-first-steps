# Product Workflow

<!-- repository-creator:managed:start -->

## Role

新規サービスのコアを完成させた後、その完成物を改めて評価し、デザイン・機能・コンテンツを必要な範囲で拡充する一気通貫の反復工程を定める。領域ごとの判断は各専門文書に従う。

## Progress Control

- 作業計画を `コア定義 → コア実装 → コア検証 → 完成後評価 → 候補精査 → 拡充実装 → 全体検証 → 再評価` の順に分け、一度に一段階だけを進行中にする。
- 前段階の完了条件を満たすまで次へ進まない。不合格なら同じ段階で修正と再検証を行う。
- コア実装中に行った改善を、完成後評価や拡充の実績として数えない。第1段階と第2段階を一続きの実装へ圧縮しない。
- 各段階の結果を進捗として明示する。ユーザー判断、追加権限、外部調整が必要でない限り、段階間の承認待ちは挟まず最後まで進める。

## Stage 1: Core Product

### Core Definition

- 対象ユーザー、解決する中心課題、中心価値、コア機能、コアコンテンツ、主要フロー、完了条件を定める。
- 最初から周辺機能や大量コンテンツへ拡散せず、中心価値を入口から結果まで体験できる最小のまとまりへ集中する。

### Implementation and Verification

- コア機能、コアコンテンツ、必要最低限のUI / UXを、分断されていない主要フローとして実装する。
- 主要フローを実際に操作し、保存、空、読込中、成功、失敗、取消・復帰など重要な状態を確認する。
- ビルド成功、HTTP 200、初期画面表示だけでは合格にしない。不足を修正し、主要フローを再検証する。
- 中心価値が実用可能で、重大な未完成を第2段階へ持ち越さない場合だけ、Stage 1の完了を明示する。

## Stage 2: Product Maturity Loop

### 1. Independent Audit

- Stage 1完了後の実物を新しい基準点として、実装とは独立して評価する。この評価前にStage 2の実装を始めない。
- `DESIGN.md`、`FEATURES.md`、`CONTENT.md` それぞれについて、現在の完成度を次のプロダクト状態へ進める重要な候補を分けて列挙する。
- UIがある場合は、`DESIGN.md` に従い、サイトなら実在する参考サイト、アプリなら実在する参考アプリをAI自身が調査・分析してからデザイン候補を確定する。一般知識だけで代替しない。
- 将来別の専門文書が追加された場合も、関係する領域を同じ方法で監査する。

### 2. Selection

- 各候補を、ユーザー価値、中心体験との接続、品質、実装・検証可能性、複雑化、維持負担で比較する。
- 各候補を `採用`、`保留`、`見送り` に分け、理由を記録する。
- デザイン候補では、参考元と、色彩、余白、版面率、画面占有率、テキスト密度など何を参考にするかを明示する。
- 採用候補を細かな一項目や一テーマへ限定せず、次の完成状態に必要なまとまりとして一つの `Expansion Set` に統合し、完了条件を定める。
- 候補一覧、採否、`Expansion Set` を進捗上のチェックポイントとして明示してから実装する。

### 3. Expansion and Verification

- `Expansion Set` を既存の機能・コンテンツ・利用体験へ接続した一つの改善として実装する。
- 主要フロー、変更・追加したフロー、重要な失敗系、既存部分との接続を実際に検証し、不合格なら修正と再検証を繰り返す。
- UIがありブラウザ操作やスクリーンショットを利用できる場合は、主要フローの実操作とスマートフォン幅・PC幅のVisual QAを行う。
- `DESIGN.md` を使用した場合は、参考元と比較可能な画面・状態で成果物を撮影し、記録した観点を比較する。重大な差があれば修正・再撮影・再比較する。
- 利用できない確認は合格扱いにせず、未検証として明示する。

### 4. Re-audit

- 検証合格後、変更後のプロダクト全体を再び独立して評価する。実装直後の動作確認だけで完了にしない。
- 次に採用すべき重要な候補があれば、新しい `Expansion Set` を選定し、実装、検証、再評価を繰り返す。
- 固定回数で終了しない。重要な採用候補がなく、追加価値より複雑化や維持負担が大きくなる場合だけ完了する。

## Records

- 候補一覧、採否、検証結果は作業計画と進捗に置き、`DESIGN.md` が求める参考資料を除いて履歴用文書を増やさない。
- 現在の成熟度、次の完成状態、`Expansion Set`、長く維持すべき判断だけを、関係する専門文書のプロジェクト固有部分へ反映する。

<!-- repository-creator:managed:end -->
